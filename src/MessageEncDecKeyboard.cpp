//
// Created by alonam on 1/14/17.
//

using namespace std;
#include <string>
#include <iostream>
#include "MessageEncDecKeyboard.h";

void MessageEncDecKeyboard::readFromKeyboard(string line) {
    string _command="";
    string _name="";
    for (int i = 0; i < line.size(); i = i + 1) {
        char c = line.at((unsigned long) i);
        if ()
    }
}
